{
  "name": "schoolbridge",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "test:e2e": "turbo test:e2e",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "db:migrate": "pnpm --filter @schoolbridge/db migrate",
    "db:generate": "pnpm --filter @schoolbridge/db generate"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0",
    "vite-tsconfig-paths": "^5.1.0",
    "vitest": "^3.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
